#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @a: the first parameter
 *
 * @b: the second parameter
 *
 * Rerurn: the sum of parameter
 */
int add(int a, int b)
{
int add;
add = a + b;
return (add);
}
