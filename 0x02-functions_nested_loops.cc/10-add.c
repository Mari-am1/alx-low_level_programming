#include "main.h"

/**
* add - adds two integers and returns the result
* @h: int to be added to m
* @m: int to be added to h
* Return: sum of h and m
*/
int add(int h, int m)
{
int sum;
sum = h + m;
return (sum);
}
